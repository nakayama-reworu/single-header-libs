cmake_minimum_required(VERSION 3.16)
project(playground)

set(CMAKE_C_STANDARD 11)

set(DYNARRAY_DEMO_NAME ${PROJECT_NAME}-dynarray-demo)
add_executable(${DYNARRAY_DEMO_NAME} vector/vector_rpn_demo.c vector/vector.c)
target_link_libraries(${DYNARRAY_DEMO_NAME} m)
target_compile_options(${DYNARRAY_DEMO_NAME} PRIVATE -Wall -Werror -Wextra -Wpointer-arith)
target_include_directories(${DYNARRAY_DEMO_NAME} PRIVATE ${CMAKE_SOURCE_DIR})
if(CMAKE_BUILD_TYPE STREQUAL "Debug")
    target_compile_definitions(${DYNARRAY_DEMO_NAME} PRIVATE DEBUG)
endif()

set(HASHTABLE_DEMO_NAME ${PROJECT_NAME}-hashtable-demo)
add_executable(${HASHTABLE_DEMO_NAME} hashtable/hashtable_demo.c vector/vector.c hashtable/hashtable.c)
target_link_libraries(${HASHTABLE_DEMO_NAME} m)
target_compile_options(${HASHTABLE_DEMO_NAME} PRIVATE -Wall -Werror -Wextra -Wpointer-arith)
target_include_directories(${HASHTABLE_DEMO_NAME} PRIVATE ${CMAKE_SOURCE_DIR})
if(CMAKE_BUILD_TYPE STREQUAL "Debug")
    target_compile_definitions(${HASHTABLE_DEMO_NAME} PRIVATE DEBUG)
endif()

set(TESTING_DEMO_NAME ${PROJECT_NAME}-testing-demo)
add_executable(${TESTING_DEMO_NAME} testing/testing_demo.c)
target_link_libraries(${TESTING_DEMO_NAME} m)
target_compile_options(${TESTING_DEMO_NAME} PRIVATE -Wall -Werror -Wextra -Wpointer-arith)
target_include_directories(${TESTING_DEMO_NAME} PRIVATE ${CMAKE_SOURCE_DIR})
if(CMAKE_BUILD_TYPE STREQUAL "Debug")
    target_compile_definitions(${TESTING_DEMO_NAME} PRIVATE DEBUG)
endif()

set(LOGGING_DEMO_NAME ${PROJECT_NAME}-logging-demo)
add_executable(${LOGGING_DEMO_NAME} logging/logging_demo.c)
target_link_libraries(${LOGGING_DEMO_NAME} m)
target_compile_options(${LOGGING_DEMO_NAME} PRIVATE -Wall -Werror -Wextra -Wpointer-arith)
target_include_directories(${LOGGING_DEMO_NAME} PRIVATE ${CMAKE_SOURCE_DIR})
if(CMAKE_BUILD_TYPE STREQUAL "Debug")
    target_compile_definitions(${LOGGING_DEMO_NAME} PRIVATE DEBUG)
endif()

#set(LINKEDLIST_DEMO_NAME ${PROJECT_NAME}-linkedlist-demo)
#add_executable(${LINKEDLIST_DEMO_NAME} linkedlist/linkedlist_demo.c linkedlist/linkedlist.c)
#target_link_libraries(${LINKEDLIST_DEMO_NAME} m)
#target_compile_options(${LINKEDLIST_DEMO_NAME} PRIVATE -Wall -Werror -Wextra -Wpointer-arith)
#target_include_directories(${LINKEDLIST_DEMO_NAME} PRIVATE ${CMAKE_SOURCE_DIR})
#if(CMAKE_BUILD_TYPE STREQUAL "Debug")
#    target_compile_definitions(${LINKEDLIST_DEMO_NAME} PRIVATE DEBUG)
#endif()
#
#set(STRING_DEMO_NAME ${PROJECT_NAME}-lisp-demo)
#add_executable(${STRING_DEMO_NAME} lisp/lisp_demo.c vector/vector.c hashtable/hashtable.c)
#target_link_libraries(${STRING_DEMO_NAME} m)
#target_compile_options(${STRING_DEMO_NAME} PRIVATE -Wall -Werror -Wextra -Wpointer-arith)
#target_include_directories(${STRING_DEMO_NAME} PRIVATE ${CMAKE_SOURCE_DIR})
#if(CMAKE_BUILD_TYPE STREQUAL "Debug")
#    target_compile_definitions(${STRING_DEMO_NAME} PRIVATE DEBUG)
#endif()

add_executable(result result/result.c)
target_include_directories(result PRIVATE ${CMAKE_SOURCE_DIR})
