cmake_minimum_required(VERSION 3.16)
project(playground)

set(CMAKE_C_STANDARD 11)

set(DYNARRAY_DEMO_NAME ${PROJECT_NAME}-dynarray-demo)
add_executable(${DYNARRAY_DEMO_NAME} dynarray/dynarray_demo.c dynarray/dynarray.c)
target_link_libraries(${DYNARRAY_DEMO_NAME} m)
target_compile_options(${DYNARRAY_DEMO_NAME} PRIVATE -Wall -Werror -Wextra)
target_include_directories(${DYNARRAY_DEMO_NAME} PRIVATE ${CMAKE_SOURCE_DIR})
if(CMAKE_BUILD_TYPE STREQUAL "Debug")
    target_compile_definitions(${DYNARRAY_DEMO_NAME} PRIVATE DEBUG)
endif()

set(HASHTABLE_DEMO_NAME ${PROJECT_NAME}-hashtable-demo)
add_executable(${HASHTABLE_DEMO_NAME} hashtable/hashtable_demo.c dynarray/dynarray.c hashtable/hashtable.c)
target_link_libraries(${HASHTABLE_DEMO_NAME} m)
target_compile_options(${HASHTABLE_DEMO_NAME} PRIVATE -Wall -Werror -Wextra)
target_include_directories(${HASHTABLE_DEMO_NAME} PRIVATE ${CMAKE_SOURCE_DIR})
if(CMAKE_BUILD_TYPE STREQUAL "Debug")
    target_compile_definitions(${HASHTABLE_DEMO_NAME} PRIVATE DEBUG)
endif()
